// stitches.config.js
import { createStyled } from '@stitches/react';

export const { styled, css, globalCss, keyframes } = createStyled({
  prefix: '',
  tokens: {},
  breakpoints: {},
  utils: {}
})
export const style = styled;